/**
 * CardItem — Jan-inspired settings row primitive.
 *
 * Re-exported from JanCard.tsx so callers can do:
 *   import { CardItem } from "@/components/settings/CardItem";
 * without needing to know both primitives live in JanCard.tsx.
 */
export { CardItem } from "./JanCard";
